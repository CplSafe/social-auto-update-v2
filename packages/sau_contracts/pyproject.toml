[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sau-contracts"
version = "0.1.0"
description = "Shared task and queue names between Dify api and sau."
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/sau_contracts"]
